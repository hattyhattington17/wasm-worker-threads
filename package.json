{
  "name": "blog-demo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "web.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build-node": "cargo +nightly build --target wasm32-unknown-unknown && wasm-bindgen --target nodejs --out-dir pkg target/wasm32-unknown-unknown/debug/blog_demo.wasm",
    "build-web": "cargo +nightly build --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir pkg target/wasm32-unknown-unknown/debug/blog_demo.wasm",
    "web": "npm run build-web && npx serve .",
    "node": "npm run build-node && node nodejs.cjs",
    "wasm2wat": "wasm2wat pkg/blog_demo_bg.wasm -o blog_demo_bg.wat"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.0"
  }
}
